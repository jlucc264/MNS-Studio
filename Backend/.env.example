ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,https://yourdomain.com,https://www.yourdomain.com
FINALIZED_REPORT_RECIPIENT=john@mns.studio
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
SUPABASE_STORAGE_BUCKET=mns-finalized-pdfs
SUPABASE_INTERNAL_STORAGE_BUCKET=mns-internal-finalized-pdfs

# Google Cloud Vision, used only for reverse-image screening of gallery
# listings (WEB_DETECTION). Optional: without it publishing still works and
# the admin screening button reports that it is not configured — listings are
# then marked unscreened rather than clear. Billing note: the first 1,000
# checks each month are free, then $3.50 per 1,000.
GOOGLE_VISION_API_KEY=
